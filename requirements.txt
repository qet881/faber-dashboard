# Keep Streamlit Community Cloud builds reproducible.
# The app was rebuilt after a data-only commit, so avoid pulling same-day
# framework/runtime releases that were not part of the last successful build.
streamlit==1.59.1
finance-datareader==0.9.202
pandas==2.2.3
numpy==2.2.6
pytz
python-dateutil
plotly
fredapi
requests
openpyxl
yfinance==1.4.1
